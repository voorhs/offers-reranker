# Реранкер офферов

Реранкер офферов служит цели отсортировать поисковую выдачу в каталоге товаров. Сейчас на моей работе есть сервис, выкаченный в продакшн, в нем в данный момент задачу фильтрации и сортировки выполняет LLM. В данном проекте я обучаю BERT-like кросс-энкодер, чтобы заменить сложный и тяжелый шаг вызова LLM.

## Данные

Я [выгрузил](./etl) входы и выходы LLM из системы инструментирования ([Logfire](https://logfire.pydantic.dev/docs/)), которую мы используем на работе, и сконвертировал в формат, пригодный для обучения модели ранжирования. Каждый семпл это:
- текстовый поисковый запрос
- список пар "оффер - золотой скор релевантости"

## Пайплайн

Запустить обучение модели можно с помощью dvc.

1. Инициализировать python среду: `uv sync`
2. Подключиться к удаленному объектному хранилищу ([инструкция](https://doc.dvc.org/user-guide/data-management/remote-storage)), креденшелы просите у админа (@voorhs)
3. Подтянуть все необходимые файлы: `uv run dvc pull`
4. Запустить пайплайн: `uv run dvc repro`
5. Отгрузить результаты пайплайна: `uv run dvc push`

## MLflow Tracking

Проект использует MLflow для отслеживания экспериментов.

### Локальный режим (по умолчанию)

```bash
cd training
uv run mlflow ui
```

### Удалённый сервер

Для подключения к удалённому MLflow серверу задайте переменные окружения:

```bash
# Tracking server
export MLFLOW_TRACKING_URI="http://mlflow-server:5001"

# Для S3/MinIO хранилища артефактов (если используется)
export AWS_ACCESS_KEY_ID="your-access-key"
export AWS_SECRET_ACCESS_KEY="your-secret-key"
export MLFLOW_S3_ENDPOINT_URL="http://minio-server:9000"
```

> **Note:** Для локальной отладки доступен референсный `docker-compose.mlflow.yaml`.
> Production-сервер должен быть настроен администраторами.

### Что логируется

| Этап | Параметры | Метрики | Артефакты |
|------|-----------|---------|-----------|
| train | model_name, lr, batch_size, num_epochs, ... | train_loss, eval_ndcg@k (autolog) | config.yaml, model/, dvc.lock |
| evaluate | model_path, test_path | test_ndcg@1/3/5, test_mrr | metrics.json |

## Простейший инференс

Смотрите [predict](./predict/).

## Целевые метрики

- NDCG@3 > 0.6
- Среднее время отклика сервиса ≤ 0.8 с

## План

отсечка 1:
- [x] собрать данные
- [x] реализовать обучение, отладить на легком бекбоне (rubert-tiny)
- [x] юнит тесты для обучения
- [x] инференс на mosec
- [x] добавить gh actions с ruff, mypy, pytest

потом:
- [ ] интеграционные тесты для обучения
- [ ] интеграционные тесты инференса
- [ ] обернуть в контейнер
- [ ] обучить более тяжелый бекбон
- [ ] посчитать финальные метрики
